name: Pylint

on: [pull_request, push]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.9", "3.10"]
    steps:
    - uses: actions/checkout@v3
    - run: echo "test"
    - name: set env as secret
      env: 
        MY_VAL: ${{ secrets.BLA_BLA }}
      run: env
